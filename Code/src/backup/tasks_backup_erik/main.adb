with Tasks.Sense; use Tasks;
with MicroBit.Console; use MicroBit.Console;

procedure Main is
begin
   -- Kjører i en loop for å kontinuerlig hente og vise sensoravstander
      loop
         null;
         -- Just a loop that does nothing.

      --  delay 1.0;  -- Vent ett sekund før neste avlesning
      delay 1.0;
   end loop;
end Main;
